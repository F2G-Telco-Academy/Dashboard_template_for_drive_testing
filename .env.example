# Template only — DO NOT put real values here (this file is committed to git).
#
# Only needed to point at a NEW Supabase project or rotate the key:
#   copy this file to ".env" (git-ignored), fill in real values, then run:
#   node scripts/generate-supabase-config.js
# The committed supabase-config.js already works for normal use.
SUPABASE_URL=your_supabase_project_url
SUPABASE_ANON_KEY=your_supabase_anon_key
